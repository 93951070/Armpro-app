/*
 * Copyright (c) 2021. Armadillo
 * Material3 品牌色板（从 XML 资源迁移至 Compose 代码）
 */

package armadillo.studio.ui.compose.theme

import androidx.compose.ui.graphics.Color

// ---- 品牌主色 ----
val Primary = Color(0xFF1A237E)          // 主色：深靛蓝
val PrimaryVariant = Color(0xFF283593)   // 主色变体
val Accent = Color(0xFFFF6F00)           // 强调色：琥珀色

// ---- 背景与表面 ----
val Background = Color(0xFFF5F5F5)       // 应用背景
val Surface = Color(0xFFFFFFFF)          // 卡片/表面背景
val SurfaceVariant = Color(0xFFEEEEEE)   // 次级表面

// ---- 文本 ----
val TextPrimary = Color(0xFF212121)       // 主文本
val TextSecondary = Color(0xFF757575)     // 次级文本

// ---- 任务状态色 ----
val StatusSuccess = Color(0xFF2E7D32)    // 成功：绿
val StatusFail = Color(0xFFC62828)       // 失败：红
val StatusWaiting = Color(0xFFFF6F00)   // 等待中：琥珀
val StatusRunning = Color(0xFF1A237E)    // 运行中：主色

// ---- 其他 ----
val OutlineColor = Color(0xFFBDBDBD)     // 描边
val OutlineVariant = Color(0xFFE0E0E0)   // 次级描边
val ErrorColor = Color(0xFFD32F2F)      // 错误
val OnErrorColor = Color(0xFFFFFFFF)     // 错误上的文本
val ScrimColor = Color(0xFF000000)        // 遮罩
